#+title: Ps Total
#+author: Erik An
#+email: obluda2173@gmail.com
#+date: <2026-02-18>
#+lastmod: <2026-02-18 13:32>
#+latex: \newpage
#+latex_header: \setlength{\parindent}{0pt}
#+latex_header: \setlength{\parskip}{1em}
#+latex_header: \usepackage{amsmath}
#+latex_header: \usepackage{amssymb}
#+latex_header: \usepackage{amsthm}
#+latex_header: \usepackage[margin=1in]{geometry}
#+options: num:t
#+startup: overview latexpreview inlineimages

* Ps 01
** Problem 1 :DONE:
Verifying the definition of a field

Show that $mathbb{F}_{2}$ is indeed a field (see example II.1.5 in the lecture notes).
** Problem 2 :DONE:
Properties of fields

In the remainder of the course, we will use field arithmetic operations rather casually, tacitly relying on the respective axioms and their consequences instead of explicitly citing them to justify every elementary arithmetic transformation. Even for completely new specific or even abstract sets and operations, that will be very similar to how you have been using arithmetic operations over natural, integer, rational and real numbers in your mathematical journey so far. This problem, however, is different: we will practice the explicit use of the definition of a field, presenting arguments in elementary steps, each relying on and explicitly justified by a single stipulation of the definition (an axiom or another detail of the definition). Examples of such steps are logical implications and arithmetic transformations, and each single one should be elementary in the above sense. Section II.1(ii) of the lecture notes presents examples of arguments structured into such elementary steps. An exception: in each problem part, a step relying on a single statement claimed to be true in a preceding problem part is considered elementary, i.e., statements from preceding problem parts may be used (with a proper reference).

Show the following properties of a field $mathbb{F}$ with operations of addition and multiplication and with additive and multiplicative identity elements $0$ and $1$.

(a) Every element has a unique additive inverse.
(b) Every element distinct from $0$ has a unique multiplicative inverse.
(c) For any $alpha, beta, gammainmathbb{F}$, $alpha boxplus gamma = beta boxplus gamma$ implies $alpha=beta$.
(d) The implication $(gamma boxplus alpha = gamma boxplus beta) Rightarrow (alpha = beta)$ holds for any $alpha, beta, gammainmathbb{F}$.
(e) The statements $alpha boxtimes gamma = beta boxtimes gamma$ and $alpha=beta$ are equivalent for any $alpha,betainmathbb{F}$ and $gammainmathbb{F}backslash{0}$.
(f) Consider $alpha,beta,gammainmathbb{F}$ such that $gammane0$ and $gamma boxtimes alpha = gamma boxtimes beta$. Then $alpha=beta$.
(g) Every $alphainmathbb{F}$ fulfills $0 boxtimes alpha = 0$. Hint: this statement expresses a non-symmetric relation between the two field operations, so the proof clearly has to use one or several of the field axioms that introduce dissimilarities of the operations.
(h) Let $-1$ denote the additive inverse of $1$ in $mathbb{F}$. Then every $alpha in mathbb{F}$ and the additive inverse $(-alpha)$ of $alpha$ satisfies $-alpha=(-1) boxtimes alpha$. Does your solution prove the uniqueness of the additive inverses? Does your solution rely on the uniqueness of the additive inverses?
(i) Additive inversion is an /involution/: if $beta$ is an additive inverse of $alphainmathbb{F}$ and $gamma$ is an additive inverse of $beta$, then $alpha=gamma$. Hint: consider the expression $alpha boxplus beta boxplus gamma$.
** Problem 3
Discrete cosine and Fourier transforms

Introduction. In several exercise sheets, we will be exploring the discrete cosine and Fourier transforms (DCTs and DFT). Apart from being intimately connected with and serving as approximations to the (real) trigonometric and (complex) Fourier series, which you will study in the Analysis course, such transforms are instrumental for interpolation with trigonometric and algebraic polynomials (we will come to that later in the course). That makes them an important topic in numerical mathematics and numerous applications, including signal and image processing. In fact, you must have been unwittingly using DCT-II daily for a long time now: the transform underlies the JPEG standard for image compression and the MPEG and H.26x standards for video compression.

In this assignment, we start with the DFT and investigate the similarity in how the DFT maps the two halves of the input column vector into the two halves of the output column vector. This will later lead us a fast DFT algorithm, known as the fast Fourier transform (FFT). The somewhat more modern term quantum Fourier transform (QFT) refers to a realization of the same FFT algorithm on a quantum computer. Apart from the challenges associated with quantum hardware and from the unfortunate notations favored by physicists due to historical reasons, the QFT algorithm itself has nothing quantum in it and perfectly falls into the scope of our course.

To the point. For a data dimension $ninmathbb{N}$, the DFT involves the matrix $Finmathbb{C}^{ntimes n}$ defined entrywise as follows:
$$F_{ij}=exp[ifrac{2pi}{n}(i-1)(j-1)] text{ for all } i,jin{1,...,n}, quad (1)$$
where $i$ is the imaginary unit (see § 1.2 in the lecture notes).

For $minmathbb{N}$ and $n=2m,$ we consider the matrix $Finmathbb{C}^{ntimes n}$ given by (1). Our goal is to relate this matrix to the smaller matrix $hat{F}inmathbb{C}^{mtimes m}$ given by (1) with $m$ in place of $n$. To this end, we select four submatrices of size $mtimes m$ in $F$ as follows. For all $alpha,betain{1,2}$ the submatrices $F_{alphabeta}$ are formed by the $m$ odd (for $alpha=1$) or by the $m$ even (for $alpha=2$) rows and by the first $m$ (for $beta=1)$ or by the last $m(beta=2)$ columns of $F$. In each case, the rows and columns are selected in the original order, as they follow in $F$. Formalizing this definition is part of this exercise.

For every $kin{1,...,m}$, we set
$$e_{k}=expfrac{ipi(k-1)}{m} quad (2)$$

(a) Verify the following equalities for all $k,jin{1,...,m}$
$$(F_{11})_{kj}=hat{F_{kj}} quad (3a)$$
$$(F_{21})_{kj}=e_{j}cdothat{F_{kj}} quad (3b)$$
$$(F_{12})_{kj}=(F_{11})_{kj}, quad (3c)$$
$$(F_{22})_{kj}=-(F_{21})_{kj}. quad (3d)$$
Hint: rewrite (3a)-(3d) for arbitrary $k,jin{1,...,m}$ in an equivalent explicit form not using the matrices introduced above. Prove the resulting equalities for arbitrary $k, jin{1,...,m}$ using properties of the complex exponential function (see § 1.2(vii) in the lecture notes).

(b) Rewrite equalities of (3a)-(3d) with $k,jin{1,...,m}$ as four matrix equalities (involving matrix multiplication and auxiliary matrices where necessary but not involving individual entries) expressing $F_{11}$, $F_{21}$, $F_{12}$ and $F_{22}$ in terms of $hat{F}$.

(c) Implement a Julia function dft matrix that, for any (reasonable) $linmathbb{N}$ produces the DFT matrix $F=$ dft matrix(l) of order $n=2^{l}$ as defined by (1).

(d) For $l=3$, $n=2^{l}$ and $m=2^{l-1}$, use dft matrix to form the DFT matrices $F$ and $hat{F}$ of orders $n$ and $m$, extract the submatrices $F_{11},$ $F_{21}$, $F_{12}$ and $F_{22}$ of $F$ and verify (3a)-(3d). Hint: to verify that two matrices are equal, you can evaluate their difference and use the function norm from the package LinearAlgebra.

(e) Repeat part (d) for $l=8$
* Ps 02
** Problem 4
Prove the distributivity of matrix multiplication over matrix addition in the case of the multiplication of a sum of two matrices by a third matrix on the left (item(i) of part (c) of Proposition II.4.10 in the lecture notes): $P(A+B)=P A+P B$ for all $A, B in mathbb{F}^{m times n}$ and $P in mathbb{F}^{p times m}$ with any $m, n, p in mathbb{N}$. Present your proof as a sequence of elementary steps, each justified with a precise verbal ${ }^{1}$ reference to a single definition, definition part or proven property from the lecture notes.
** Problem 5
Consider the following matrices over the field $mathbb{R}$ of real numbers:

$$
u=left[begin{array}{r}
2
1
-1
3
end{array}right], quad A=left[begin{array}{rr}
-2 & 4
1 & 0
3 & 1
2 & 3
end{array}right] quad B=left[begin{array}{rr}
4 & 0
1 & -2
-2 & 0
1 & -2
2 & 3
end{array}right], quad text { and } quad v=left[begin{array}{r}
-1
2
1
4
-1
end{array}right]
$$

In some of the problem parts below, you are asked to count the additions and multiplications of scalars (all of which happen to be integers in this fortunate example) involved in the respective calculations. In each case, the actual evaluations are meant, and each is to be counted once (for example, how many times you write any single operation or batch of operations on paper is irrelevant for those counts).

(a) (1) Write $u^{top}$ and $B^{top}$ explicitly in the "table" notation (as the original matrices are given above).

(b) (3) Evaluate the product $C=A B^{top}$ column-wise (using rule (a) of Remark II.4.5 of the lecture notes). In your solution, show the assembly of all the linear combinations involved (what is combined and with what coefficients, as that is done for the third column of $A$ in Example II.4.6 in the lecture notes).
Find the numbers $N_{+}$ and $N_{times}$ of additions and multiplications involved in the evaluation of $C$ in this part.

(c) (3) Evaluate the product $C=A B^{top}$ row-wise (using rule (b) of Remark II.4.5 of the lecture notes). In your solution, show the assembly of all the linear combinations involved (what is combined and with what coefficients, as that is done for the third row of $A$ in Example II.4.6 in the lecture notes). The result should agree with that of part (b).
Find the numbers $N_{+}$ and $N_{times}$ of additions and multiplications involved in the evaluation of $C$ in this part.

(d) (2) Using the matrix $C$ obtained in parts (b) and (c), evaluate the product $u^{top} C v$ (strictly speaking, a matrix of size $1 times 1$).
Find the numbers $N_{+}$ and $N_{times}$ of additions and multiplications involved in the evaluation of $u^{top} C v$ in this part.

(e) (2) Evaluate the product $xi=u^{top} A$. Find the numbers $N_{+}$ and $N_{times}$ of additions and multiplications involved in the evaluation of $xi$ in this part.

(f) (2) Evaluate the product $eta=B^{top} v$. Find the numbers $N_{+}$ and $N_{times}$ of additions and multiplications involved in the evaluation of $eta$ in this part.

(g) (2) Evaluate the product $xi eta$. Must or must not the result agree with that of part (d)? Why?
Find the numbers $N_{+}$ and $N_{times}$ of additions and multiplications involved in the evaluation of $xi eta$ in this part.

(h) (1) Which of the above ways of calculating $u^{top} A B^{top} v$ is more efficient? Why?
** Problem 6
We continue the treatment of the discrete cosine and Fourier transforms. For a data dimension $n in mathbb{N}$, some of the DCTs and the DFT involve the following matrices $B, C in mathbb{R}^{n times n}$ and $F in mathbb{C}^{n times n}$:

begin{gathered}
B_{i j}=cos left[frac{pi}{n}left(i-frac{1}{2}right)left(j-frac{1}{2}right)right], quad C_{i j}=cos left[frac{pi}{n}left(i-frac{1}{2}right)(j-1)right]
F_{i j}=exp left[mathrm{i} frac{2 pi}{n}(i-1)(j-1)right]
end{gathered}

(a) (5) Using trigonometric identities, calculate $G=B^{top} B$. Find a matrix $X in mathbb{R}^{n times n}$ such that $G=X^{2}$.

Hint: for those lost in the world of trigonometric identities, it may be helpful to consider the following two as basic ones (at least, for this problem):

$$
cos (alpha+beta)=cos alpha cos beta-sin alpha sin beta quad text { and } quad cos left(alpha+frac{pi}{2}right)=-sin alpha
$$

for all $alpha, beta in mathbb{R}$. In fact, the second identity is a particular case of the first. We add to the above two identities also the parity of the cosine and sine functions:

$$
cos (-alpha)=cos alpha quad text { and } quad sin (-alpha)=-sin alpha
$$

for all $alpha in mathbb{R}$. The above four identities imply the following three, similar to the very first one:

begin{aligned}
& cos (alpha-beta)=cos alpha cos beta+sin alpha sin beta,
& sin (alpha+beta)=sin alpha cos beta+cos alpha sin beta,
& sin (alpha-beta)=sin alpha cos beta-cos alpha sin beta
end{aligned}

for all $alpha, beta in mathbb{R}$. To exercise, you can verify the last three identities using only the first four. Producing such proofs may feel technical and challenging, but that would mean only that you personally need to exercise a bit - in this case, by proving the last three identities using the first four.
In solving this problem, you will encounter products of the form $cos alpha cos beta$ with $alpha, beta in mathbb{R}$. Try to simplify them using some of the above equalities: a sum of trigonometric functions is usually easier to work with than a product. Finally, sums of the form

$$
sum_{k=1}^{n} cos (phi+omega k)
$$

with $phi, omega in mathbb{R}$ and $n in mathbb{N}$ are ubiquitous in the context of trigonometric and Fourier series. They are remarkable in that the arguments form an arithmetic progression, which allows to turn such a sum into a telescoping sum. To see what that means, instead of searching online, just try applying some of the above identities after multiplying the above sum of cosines by $sin frac{omega}{2}$!
Note: there is nothing particularly complicated in the above identities or in how they can be used in this problem. The idea of every single step can come entirely by expediency, not by miracle. Each time, look carefully at where you are and at the limited range of options for what you could potentially undertake (just a few identities available), choose your strategy and execute it.

(b) (3) Verify the equality $B^{top} B=X^{2}$ in Julia for $n=100$ and for $n=101$.

(c) (2) Using trigonometric identities, calculate $H=C^{top} C$. Find a matrix $Y in mathbb{R}^{n times n}$ such that $H=Y^{2}$.

(d) (3) Verify the equality $C^{top} C=Y^{2}$ in Julia for $n=100$ and for $n=101$.

(e) (5) Give an explicit expression for the entries of the matrix $F^{*}=left[overline{F_{j i}}right]_{i=1, j=1}^{n} in mathbb{C}^{n times n}$, where overline denotes complex conjugation (see§ I.2.4). The matrix $F^{*}$ is called the Hermitian conjugate of $F$.
Using properties of the complex exponential function, calculate $H=F^{*} F$. Find $Z in mathbb{C}^{n times n}$ such that $H=Z^{2}$.

Hint: using induction, check that the formula for the sum of a geometric progression holds for complex numbers (for a complex denominator, in particular).

(f) (3) Verify the equality $F^{*} F=Z^{2}$ in Julia for $n=100$ and for $n=101$.
* Ps 03
** Problem 7
(33) Discrete cosine transforms: multilevel structure

We continue the treatment of discrete cosine transforms. In this problem, you will practice:
matrix notations (§ II.3.i) and some patterned matrices (Definition II.3.8) in parts (b), (e);
permutation matrices in parts (b) and (f);
linear operations on matrices and their properties (Definition II.3.4 and II.3.5, Proposition II.3.7) in part (j);
matrix multiplication (Definition II.4.3 and Remark II.4.5) in parts (b), (d), (e), (h), (j);
matrix transposition (Definition II.3.12) in parts (h) and (j);
matrix inversion (Definition II.4.14) in parts (c) and (g);
implementation in Julia in parts (k) and (l);
trigonometry!

Pay attention to notational differences from Problem 3.
Some DCTs of order $ninmathbb{N}$ involve the matrices $B, Cinmathbb{R}^{ntimes n}$ given by
$B_{ij}=cos[frac{pi}{n}(i-frac{1}{2})(j-frac{1}{2})]$ and $C_{ij}=cos[frac{pi}{n}(i-frac{1}{2})(j-1)]$ (1)
for all $i,jin{1,...,n}$ .

For $ninmathbb{N}$ we consider the matrices $B, Cinmathbb{R}^{ntimes n}$ given by (1) and the matrices $hat{B}$, $hat{C}inmathbb{R}^{2ntimes2n}$ defined analogously, with $2n$ in place of $n$ in (1).
Our goal is to relate the larger matrices $hat{B}$ and $hat{C}$ to the smaller matrices $B$ and $C$. To this end, we select four submatrices of size $ntimes n$ in each of $hat{B}$ and $hat{C}$ as follows. For all $alpha,betain{1,2}$, the submatrices $tilde{B}_{alphabeta}$ and $tilde{C}_{alphabeta}$ (note Remark II.3.3 in the lecture notes) are formed by the first $n$ (for $alpha=1$) or by the last $n$ ($alpha=2$) rows and by the $n$ odd (for $beta=1$) or by the $n$ even (for $beta=2$) columns of $hat{B}$ and $hat{C}$, respectively.
In each case, the rows and columns are selected in the original order, as they follow in $hat{B}$ and $hat{C}$ .

We will use several auxiliary matrices, the first of which is the matrix $Ainmathbb{R}^{ntimes n}$ given by
$A_{ij}=-sin[frac{pi}{n}(i-frac{1}{2})j]$ for all $i,jin{1,...,n}$. (2)

Note: binary (yes-no) questions require justified answers (proofs) unless the contrary is explicitly stated.

(a) This problem is in part concerned with the verification of trigonometric equalities, and that work will be split among the students.
Use the following table to identify your team on the basis of your name (as it is known to u:space).

The following table:
| | last name starts with A-K | last name starts with L-Z |
|---|---|---|
| first name starts with A-K | Team 11 | Team 12 |
| first name starts with L-Z | Team 21 | Team 22 |

Do not get carried away by the idea of teamwork: your solutions should be your own, and teamwork should take place at the exercise-group meeting .

(b) Let $Jinmathbb{R}^{ntimes n}$ be the permutation matrix associated with the permutation $(n,n-1,...,2,1)$ of ${1,...,n}$ see Example II.4.40 and Definition II.4.42 in the lecture notes. Express the matrix $J$ entrywise (with ellipses) in the square-bracket notation (see the display equation in Remark II.4.8 for an example).

(c) Is the matrix $J$ invertible? If yes, find its inverse.

(d) Give explicit expressions for the entries of the matrices $Jtilde{B}_{21}$ and $Jtilde{B}_{22}$.
Give explicit expressions for the entries of the matrices $Jtilde{C}_{21}$ and $Jtilde{C}_{22}$ .

(e) Let $Sinmathbb{R}^{ntimes n}$ be a matrix of downward one-position shift:
$Sbegin{bmatrix}x_{1} x_{2} vdots x_{n-1} x_{n}end{bmatrix}=begin{bmatrix}0 x_{1} vdots x_{n-2} x_{n-1}end{bmatrix}$ for each $x=[x_{j}]_{j=1}^{n}inmathbb{R}^{n}$.
Is such a matrix $S$ unique or not?
Find one such a matrix $S$ and express it entrywise (with ellipses) in the square-bracket notation (see the display equation in Remark II.4.8 for an example) .

(f) Is the matrix $S$ you found in part (e) a permutation matrix? If yes, give the associated permutation.

(g) Is the matrix $S$ you found in part (e) invertible? If yes, find its inverse.

(h) Give explicit expressions for the entries of the matrices $CS$ and $AS^{top}$.

(i) Of the following equalities, prove the one assigned to your team.
(Team 11) $tilde{C}_{11}=C$
(Team 21) $Jtilde{C}_{21}=C$
(Team 12) $tilde{C}_{12}=B$
(Team 22) $Jtilde{C}_{22}=-B$

(j) Of the following equalities, prove the one assigned to your team, with suitable diagonal matrices $X, Yinmathbb{R}^{ntimes n}$.
(Team 21) $tilde{B}_{11}=XC+YAS^{top}$
(Team 11) $Jtilde{B}_{21}=YC-XAS^{top}$
(Team 22) $tilde{B}_{12}=XCS-YA$
(Team 12) $Jtilde{B}_{22}=YCS+XA$

Hint. Let us look at the right-hand side matrices of the above equalities.
Note that, due to (1) and (2), for every $jin{1,...,n}$, the entries in the $j$th columns of $C$ and $A$ are cosines and sines of multiples of $j-1$ and $j$, respectively (by the way, think what similar statement you can make regarding $AS^{top}$ and $CS$).
So you may want to use trigonometric identities (see Problem 6 for a sufficient set of such identities) to express the entries in the $j$th columns of the left-hand side matrices from the above equalities as trigonometric functions of $(*)+(**)$, where $(*)$ denotes multiples of $j-1$ (for $tilde{B}_{11}$ and $Jtilde{B}_{21}$) or of $j$ (for $tilde{B}_{12}$ and $Jtilde{B}_{22}$) and $(**)$ denotes quantities independent of $j$, and then apply suitable identities to split $(*)$ and $(**) $.

(k) Verify all the four equalities of part (i) in Julia for $n=100$ and for $n=101$.

(l) Using the matrices $X$ and $Y$ that you found in (your assigned equality of) part (j), verify all the four equalities of part (j) in Julia for $n=100$ and for $n=101$.

Side remark: high-school mathematics establishes a bad reputation for trigonometry, but see what a symmetry in $hat{B}$ it reveals here!
** Problem 8
Permutation matrices

(a) Let $ninmathbb{N}$ and $Piinmathbb{F}^{ntimes n}$ be a permutation matrix. In terms of matrix multiplication, prove that there exist unique indices $pi_{k}in{k,...,n}$ with $kin{1,...,n}$ such that $Pi=Pi_{n}cdotcdotcdot Pi_{1}$ where each $Pi_{k}$ with $kin{1,...,n}$ is the $(k,pi_{k})$ -exchange matrix of order $n$ (see Definition II.4.44).

(b) Count the number of permutation matrices of order $ninmathbb{N}$.
** Problem 9
Small data and big data

Let $ninmathbb{N}$ be such that $nge2025$. For each of the following matrices, find a rank-$r$ factorization with the least possible $rinmathbb{N}$ and prove $r$ to be the least possible.
Hint: to show the minimality of a factorization rank $rinmathbb{N}$, assume that a factorization of rank $r-1$ exists and arrive at a contradiction.

(a) $A=begin{bmatrix}1&0 0&0end{bmatrix}inmathbb{R}^{2times2}$

(b) $A=begin{bmatrix}-1&1 0&1end{bmatrix}inmathbb{R}^{2times2}$

(c) $Ainmathbb{R}^{ntimes n}$ given by $A_{ij}=i$ for all $i,jin{1,...,n}$.

(d) $Ainmathbb{R}^{ntimes n}$ given by $A_{ij}=i-j$ for all $i,jin{1,...,n}.$

(e) $Ainmathbb{R}^{ntimes n}$ given by
$A_{ij}=i^{2}+cosfrac{pi j}{n}-cosfrac{pi i}{n}$
for all $i, jin{1,...,n}$
* Ps 04
** Problem 10 (a) :DONE:
In this exercise, we work over the field $mathbb{R}$. Consider
$A=begin{bmatrix}2&1&-1 4&5&-3 -2&5&-2end{bmatrix}inmathbb{R}^{3times3}$,
$b=begin{bmatrix}1 7 3end{bmatrix}inmathbb{R}^{3}$.
Perform Gaussian elimination on the system $Ax=b$ without row exchanges. Show explicitly how you eliminate the entries below the diagonal in the first and second columns, and write down the resulting upper-triangular matrix $U$.
** Problem 10 (b) :DONE:
Let $m_{21}$, $m_{31}$ be the multipliers used to eliminate the entries in the first column, and let $m_{32}$ be the multiplier used to eliminate the (3, 2)-entry after the first elimination step.
Form the unit lower-triangular matrix
$L=begin{bmatrix}1&0&0 m_{21}&1&0 m_{31}&m_{32}&1end{bmatrix}$
Verify that your matrices satisfy $A=LU$.
** Problem 10 (c) :DONE:
Solve $Ax=b$ using the factorization $A=LU$ by (i) solving $Ly=b$ for $y$ (forward substitution) and then (ii) solving $Ux=y$ for $x$ (backward substitution). Present your intermediate vector $y$ and the final solution $x$.
** Problem 10 (d) :DONE:
(Programming, Julia) Implement two Julia functions =forward_substitution(L,b)= and =backward_substitution(U,y)= that solve $Ly=b$ band $Ux=y$ for triangular matrices of compatible sizes. Use your code to solve the system from this exercise and verify that your result agrees with Julia's A b.
** Problem 11 (a) :DONE:
Tall system. Consider
$U=begin{bmatrix}1&2&-1 0&1&3 0&0&2 0&1&1end{bmatrix}inmathbb{R}^{4times3}$ $y=begin{bmatrix}y_{1} y_{2} y_{3} y_{4}end{bmatrix}inmathbb{R}^{4}$
Write
$U=begin{bmatrix}U_{1} U_{2}end{bmatrix}$
with $U_{1}inmathbb{R}^{3times3}$ and $U_{2}inmathbb{R}^{1times3}$. Derive a necessary and sufficient condition on $y$ for the system
$$Uz=y$$
to have a solution $zinmathbb{R}^{3}$, and express the (necessarily unique) solution $z$ in terms of $y_{1}$, $y_{2}$, $y_{3}$ Then check your condition for the concrete right-hand side
$$y=begin{bmatrix}2 1 3 -2end{bmatrix}$$
** Problem 11 (b) :DONE:
Discuss why the solution to the system $Uz=y$ is necessarily unique.
** Problem 11 (c) :DONE:
Wide system. Consider
$W=begin{bmatrix}1&2&1&0 0&1&-1&3end{bmatrix}inmathbb{R}^{2times4};$
$z=begin{bmatrix}z_{1} z_{2}end{bmatrix}inmathbb{R}^{2}$.
Partition $W=begin{bmatrix}W_{1}&W_{2}end{bmatrix}$ with $W_{1}inmathbb{R}^{2times2}$ and $W_{2}inmathbb{R}^{2times2}$ and similarly split $x=$
$begin{bmatrix}x^{(1)} x^{(2)}end{bmatrix}$
with $x^{(1)}inmathbb{R}^{2}$ and $x^{(2)}inmathbb{R}^{2}$. Express the full solution set
${xinmathbb{R}^{4}:Wx=z}$
in terms of the free variables $x^{(2)}$. For the concrete right-hand side $z=begin{bmatrix}5 1end{bmatrix}$ provide two distinct solutions.
** Problem 12 (a) :DONE:
We work over $mathbb{R}$. Consider
$A=begin{bmatrix}0&2&1 1&1&0 2&3&4end{bmatrix}$
$b=begin{bmatrix}3 1 8end{bmatrix}$
Let $Pi$ be the permutation matrix that exchanges the first and second rows. Write $Pi$ explicitly and verify (by a direct calculation) that $Pi^{-1}=Pi^{T}$.
** Problem 12 (b) :DONE:
Prove that the linear systems $Ax=b$ and $(Pi A)x=Pi b$ have exactly the same solution set.
** Problem 12 (c) :DONE:
Compute an LU factorization of $Pi A$ without further row exchanges, and use it to solve $Ax=b$ (by solving $(Pi A)x=Pi b$ via forward/back substitution).
* Ps 05
** Problem 13 :DONE:
Let
$$A=begin{bmatrix}0&2&1 2&2&3 4&1&8end{bmatrix}inmathbb{R}^{3times3}, quad b=begin{bmatrix}3 7 10end{bmatrix}inmathbb{R}^{3}.$$

(a) Perform the first step of Gaussian elimination with partial pivoting in the first column, i.e., look for an appropriate row $j$ in your working column that contains a non-zero element, and then swap the first and $j$ th row. Write down the permutation matrix $P_{1}$ representing the row swap that is performed (if any), and the resulting matrix $P_{1}A$.

(b) Continue the elimination with partial pivoting until you obtain matrices $P, L, U$ such that
$$PA=LU$$,
where $P$ is a permutation matrix, $L$ is unit lower triangular, and $U$ is upper triangular. Write down $P$, $L$, and $U$ explicitly.

(c) Solve $Ax=b$ using the factorization $PA=LU$ by solving $Ly=Pb$, $Ux=y$. Present $y$ and $r$.
** Problem 14 :DONE:
(a) Let $sigma=(3,1,2)$ and $tau=(2,1)$ be permutations of ${1,2,3}$. Write down the corresponding permutation matrices $P_{sigma}$ and $P_{tau}$ acting on row vectors via left multiplication, i.e. $P_{sigma}A$ permutes the rows of $A$ by $sigma$.

(b) Compute $P_{tau}P_{sigma}$ and identify the permutation $rho$ such that $P_{tau}P_{sigma}=P_{rho}$. Explain briefly (one or two sentences) how this relates to composition of permutations.

(c) Prove that for every permutation matrix $P$ one has $P^{-1}=P^{T}$.
** Problem 15 :DONE:
Let
$$A=begin{bmatrix}3&1&1 6&3&4 3&1&5end{bmatrix}inmathbb{R}^{3times3}, quad b^{(1)}=begin{bmatrix}1 2 3end{bmatrix} quad b^{(2)}=begin{bmatrix}2 0 1end{bmatrix}$$

(a) Compute an LU factorization with pivoting, i.e. find $P$, $L$, $U$ such that $PA=LU$.

(b) Using the same $P$, $L$, $U$, solve $Ax=b^{(1)}$ and $Ax=b^{(2)}$. Show the two solutions.
** Problem 16 :DONE:
Consider
$$A=begin{bmatrix}1&2&3 2&4&6 1&1&1end{bmatrix}inmathbb{R}^{3times3}.$$

(a) Perform Gaussian elimination with partial pivoting and determine the rank of $A$ from the resulting echelon form.

(b) For the two right-hand sides
$$b^{(1)}=begin{bmatrix}1 2 1end{bmatrix} quad b^{(2)}=begin{bmatrix}1 3 1end{bmatrix}$$
decide for each system $Ax=b^{(j)}$ whether it is consistent, i.e., whether it has a solution. If consistent, describe the full solution set.
** Problem 17 :DONE:
Let $mathbb{F}in{mathbb{Q},mathbb{R},mathbb{C}}$ and assume that in step $k$ of Gaussian elimination with partial pivoting the pivot row is chosen such that
$$|a_{kk}^{(k)}|=max_{iin{k,...,n}}|a_{ik}^{(k)}|$$
where $a_{ik}^{(k)}$ denotes the entries of the matrix after the first $k-1$ elimination steps.

(a) Show that the elimination multipliers
$$l_{ik}=frac{a_{ik}^{(k)}}{a_{kk}^{(k)}}, quad i=k+1,....n,$$
satisfy $|l_{ik}|le1$.

(b) Conclude that in the resulting factorization $PA=LU$ obtained by column pivoting, all subdiagonal entries of $L$ satisfy $|L_{ik}|le1$ for $i>k$.
** Problem 18 :DONE:
A sensor produces one measurement per hour over an 8-hour cycle, yielding the data

$$y=begin{bmatrix}y_{0} y_{1} y_{2} y_{3} y_{4} y_{5} y_{6} y_{7}end{bmatrix}=
begin{bmatrix}8 7 2 4 8 6 2 3end{bmatrix} in mathbb{R}^{8}$$

A common "data science" model for periodic effects is to fit a small number of Fourier features. Here we use the constant term and the harmonic of frequency 2:
$$hat{y}_{t}=a_{0}+a_{c}cos(frac{pi t}{2})+a_{s}sin(frac{pi t}{2}) , quad t=0,1,...,7,$$
where $a_{0}$, $a_{c}$ $a_{s}inmathbb{R}$ are to be chosen such that the squared error

$$sum_{t=0}^{7}(hat{y}_{t}-y_{t})^{2}$$

is minimal.

Define the design matrix $Xinmathbb{R}^{8times3}$ by
$$X_{t+1,1}=1, quad X_{t+1,2}=cos(frac{pi t}{2}) , quad X_{t+1,3}=sin(frac{pi t}{2}), quad t=0,...,7$$
so that $hat{y}=Xa$ with $a=(a_{0},a_{c},a_{s})^{T}$.

(a) Write down $X$ explicitly by using the values

k$$cos(frac{pi t}{2})in{-1,0,1}, quad sin(frac{pi t}{2})in{-1,0,1}$$

(b) Compute $X^{T}X$ and show that it is diagonal. (Hint: You only need the sums $sum_{t=0}^{7}cos(pi t/2)$, $sum_{t=0}^{7}sin(pi t/2)$, $sum_{t=0}^{7} cos(pi t/2) sin(pi t/2)$, $sum_{t=0}^{7} cos^2 (pi t/2)$, $sum_{t=0}^{7} sin^2(pi t/2)$.

(c) Derive the (so called) normal equations
$$(X^{T}X)a=X^{T}y$$
and solve them for $a_{0}$, $a_{c}$, $u_{s}$.

(d) Compute the fitted signal $hat{y}=Xa$ and the residual $r=y-hat{y}$. Give the squared residual norm $||r||_{2}^{2}=sum_{t=0}^{7}r_{t}^{2}$.
* Ps 06
** Problem 19 :DONE:
19. (18) Linear systems of equations, Reduced row echelon form

(a) (12) Solve the following linear system of equations using Gaussian elimination, i.e., an LU-decomposition (possibly with partial pivoting)

$(2+i)  z_1 + (-1+3i) z_2 + (4-i)  z_3 + (1+2i) z_4 = 5+24i$,
$(3-2i) z_1 + (2+i)   z_2 + (-1+i) z_3 + 5      z_4  = 24 - 7i$,
$(-2+i) z_1 + (1-4i)  z_2 + (3+2i) z_3 + (-1+i) z_4 = -14 - 2i$,
$(1+3i) z_1 - 2       z_2 + (2-i)  z_3 + (4+i)  z_4 = 7 + 10i$.


(b) (6) Transform the following real $4times5$ matrix into reduced row echelon form:

$$A=begin{pmatrix}12&6&4&-7&-3 -6&-3&12&7&1 6&3&-8&-6&4 18&9&4&-11&2end{pmatrix}$$
** Problem 20 :DONE:
20. (15) Vector space

(a) (9) Let $V$ be an $mathbb{F}$ -vector space and X an arbitrary set. Consider the set $mathcal{F}(X,V)$ of all functions $f:Xrightarrow V$ with pointwise operations as defined in the lecture course. Show that $mathcal{F}(X,V)$ is a $mathbb{F}$ -vector space.

(b) (6) Let $V$ and $W$ be $mathbb{F}$ -vector spaces. Consider on the cartesian product $Vtimes W$ the following operations: For all $v,v^{prime}in V,w,w^{prime}in W$ and $lambdainmathbb{F}$ let

begin{align*}
(v,w)+(v^{prime},w*) &:= (v+v^{prime},w+w^{prime})
lambda(v,w) &:= (lambda v,lambda w)
end{align*}

Show that these two operations make $Vtimes W$ a vector space over $mathbb{F}$.
** Problem 21 :DONE:
21. (12) Subspace

We consider the vector space $mathbb{F}^{n}$ for the specified field and some $4le ninmathbb{N}$. Check, whether the following subsets are subspaces.

(a) (2) $W_{1}:={xinmathbb{R}^{n}|x_{1}+x_{2}=0}$,

(b) (2) $W_{2}:={xinmathbb{R}^{n}|sum_{j=1}^{n}jx_{j}=0}$,

(c) (2) $W_{3}:={xinmathbb{R}^{n}|x_{1}-x_{3}=x_{4}+1}$,

(d) (2) $W_{4}:={xinmathbb{R}^{n}|sum_{i=1}^{n}x_{i}^{2}=0}$,

(e) (2) R$W_{5}:={xinmathbb{F}_{2}^{3}|x_{1}^{2}+x_{2}+x_{3}^{3}=0}$,

(f) (2) $W_{6}:={xinmathbb{C}^{2}|x_{1}ne0wedgefrac{x_{2}}{x_{1}}=1}$.
** Problem 22 :DONE:
22. (12) Spanning sets, Linear Independence

(a) (2) Show that $mathbb{C}$ is a vector space over $mathbb{R}$ and find a small spanning set for $mathbb{C}$.

(b) (4) We consider the vector space $mathbb{F}^{4}$ for some field $mathbb{F}$. Show that the vectors $(1,0,0,0)^{T}$, $(1,1,0,0)^{T}$, $(1,1,1,0)^{T}$ and $(1,1,1,1)^{T}$ are linearly independent.

(c) (6) Show that the three vectors $(i, 1, 2+i)$, $(1,3i+1,2)$ and $(-1,5i, 5)$ are a spanning set of $mathbb{C}^{3}$. Are they linearly independent?
