#+title: Set 04
#+author: Erik An
#+email: obluda2173@gmail.com
#+date: <2026-01-08>
#+lastmod: <2026-01-12 11:27>
#+options: num:t
#+startup: overview
#+latex: \newpage

* Problem 10
** Task
#+begin_quote
(20) Gaussian elimination as LU factorization
In this exercise, we work over the field $\mathbb{R}$. Consider
$$A=\begin{bmatrix}2&1&-1\\ 4&5&-3\\ -2&5&-2\end{bmatrix}\in\mathbb{R}^{3\times3},$$
$$b=\begin{bmatrix}1\\ 7\\ 3\end{bmatrix}\in\mathbb{R}^{3}.$$

(a) (6) Perform Gaussian elimination on the system $Ax=b$ without row exchanges. Show explicitly how you eliminate the entries below the diagonal in the first and second columns, and write down the resulting upper-triangular matrix $U$.

(b) (4) Let $m_{21}$, $m_{31}$ be the multipliers used to eliminate the entries in the first column, and let $m_{32}$ be the multiplier used to eliminate the (3, 2)-entry after the first elimination step. Form the unit lower-triangular matrix
$$L=\begin{bmatrix}1&0&0\\ m_{21}&1&0\\ m_{31}&m_{32}&1\end{bmatrix}$$
Verify that your matrices satisfy $A=LU$.

(c) (6) Solve $Ax=b$ using the factorization $A=LU$ by (i) solving $Ly=b$ for $y$ (forward substitution) and then (ii) solving $Ux=y$ for (backward substitution). Present your intermediate vector $y$ and the final solution 2.

(d) (4) (Programming, Julia) Implement two Julia functions forward_substitution (L,b) band $Ux=y$ for triangular matrices of compatible sizes. Use your code to solve the system from this exercise and verify that your result agrees with Julia's A b.
#+end_quote

** Notes

** Solution
#+begin_src julia :results value :exports both :tangle julia/p_10.jl

#+end_src

* Problem 11
** Task
#+begin_quote
(19) Square, tall and wide systems (triangular/block criteria)
This exercise is a direct application of the criteria discussed for triangular/block systems.

(a) (8) Tall system. Consider
$$U=\begin{bmatrix}1&2&-1\\ 0&1&3\\ 0&0&2\\ 0&1&1\end{bmatrix}\in\mathbb{R}^{4\times3}, \quad y=\begin{bmatrix}y_{1}\\ y_{2}\\ y_{3}\\ y_{4}\end{bmatrix}\in\mathbb{R}^{4}.$$
Write
$$U=\begin{bmatrix}U_{1}\\ U_{2}\end{bmatrix}$$
with $U_{1}\in\mathbb{R}^{3\times3}$ and $U_{2}\in\mathbb{R}^{1\times3}$. Derive a necessary and sufficient condition on $y$ for the system
$$Uz=y$$
to have a solution $z\in\mathbb{R}^{3}$, and express the (necessarily unique) solution $z$ in terms of $y_{1}$, $y_{3}$ Then check your condition for the concrete right-hand side
$$y=\begin{bmatrix}2\\ 1\\ 3\\ -2\end{bmatrix}$$
and compute the corresponding solution $z$.

(b) (3) Discuss why the solution to the system $Uz=y$ is necessarily unique.

(c) (8) Wide system. Consider
$$W=\begin{bmatrix}1&2&1&0\\ 0&1&-1&3\end{bmatrix}\in\mathbb{R}^{2\times4};$$
$$z=\begin{bmatrix}z_{1}\\ z_{2}\end{bmatrix}\in\mathbb{R}^{2}.$$
Partition $W=\begin{bmatrix}W_{1}&W_{2}\end{bmatrix}$ with $W_{1}\in\mathbb{R}^{2\times2}$ and $W_{2}\in\mathbb{R}^{2\times2}$ and similarly split $x=$
$$\begin{bmatrix}x^{(1)}\\ x^{(2)}\end{bmatrix}$$
with $x^{(1)}\in\mathbb{R}^{2}$ and $x^{(2)}\in\mathbb{R}^{2}$. Express the full solution set
$$\{x\in\mathbb{R}^{4}:Wx=z\}$$
in terms of the free variables $x^{(2)}$. For the concrete right-hand side
$$z=\begin{bmatrix}5\\ 1\end{bmatrix}$$
provide two distinct solutions.
#+end_quote

** Notes

** Solution

* Problem 12
** Task
#+begin_quote
(11) Row exchanges, permutation matrices, and pivoting
We work over $\mathbb{R}$. Consider
$$A=\begin{bmatrix}0&2&1\\ 1&1&0\\ 2&3&4\end{bmatrix}$$
$$b=\begin{bmatrix}3\\ 1\\ 8\end{bmatrix}$$

(a) (3) Let $\Pi$ be the permutation matrix that exchanges the first and second rows. Write $\Pi$ explicitly and verify (by a direct calculation) that $\Pi^{-1}=\Pi^{T}$.

(b) (2) Prove that the linear systems $Ax=b$ and $(\Pi A)x=\Pi b$ have exactly the same solution set.

(c) (6) Compute an LU factorization of $\Pi A$ without further row exchanges, and use it to solve $Ax=b$ (by solving $(\Pi A)x=\Pi b$ via forward/back substitution).
#+end_quote

** Notes

** Solution
