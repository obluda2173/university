#+title: Ps 10
#+author: Erik An
#+email: obluda2173@gmail.com
#+date: <2026-06-11>
#+lastmod: <2026-06-19 18:21>
#+latex: \newpage
#+latex_header: \setlength{\parindent}{0pt}
#+latex_header: \setlength{\parskip}{1em}
#+latex_header: \usepackage{amsmath}
#+latex_header: \usepackage{amssymb}
#+latex_header: \usepackage{mathtools}
#+latex_header: \usepackage{amsthm}
#+latex_header: \usepackage[margin=1in]{geometry}
#+options: num:t tags:nil
#+property: header-args :eval never-export
#+startup: overview latexpreview inlineimages
#+columns: %50ITEM(Item) %8LECTURE_REF(Lecture) %34CUSTOM_ID(ID)

* Problem 90 :binary_search_tree:dynamic_programming:pseudocode:
** Task
#+begin_quote
Write pseudocode for the procedure CONSTRUCT-OPTIMAL-BST(root, n) which, given the table root[1 : n, 1 : n] produced by the function OPTIMAL-BST(p, q, n), constructs the optimal binary search tree.
#+end_quote

** Solution

#+begin_src julia :results output
function construct_optimal_bst(r, n)
    function build(i, j, parent, dir)
        if i > j
            println("d", i - 1, " is the ", dir, " child of k", parent)
            return
        end
        t = r[i, j]
        if parent == 0
            println("k", t, " is the root")
        else
            println("k", t, " is the ", dir, " child of k", parent)
        end
        build(i, t - 1, t, "left")
        build(t + 1, j, t, "right")
    end
    build(1, n, 0, "")
end

root = [1 1 2 2 2;
        0 2 2 2 4;
        0 0 3 4 5;
        0 0 0 4 5;
        0 0 0 0 5]

construct_optimal_bst(root, 5)
#+end_src

#+RESULTS:
#+begin_example
k2 is the root
k1 is the left child of k2
d0 is the left child of k1
d1 is the right child of k1
k5 is the right child of k2
k4 is the left child of k5
k3 is the left child of k4
d2 is the left child of k3
d3 is the right child of k3
d4 is the right child of k4
d5 is the right child of k5
#+end_example

* Problem 91 :binary_search_tree:dynamic_programming:
** Task
#+begin_quote
Determine the cost and structure of an optimal binary search tree for the keys $k_i$ and failed searches $d_i$:

$$\begin{array}{c|cccccccc}
i   & 0    & 1    & 2    & 3    & 4    & 5    & 6    & 7    \\
\hline
p_i &      & 0.04 & 0.06 & 0.08 & 0.02 & 0.10 & 0.12 & 0.14 \\
q_i & 0.06 & 0.06 & 0.06 & 0.06 & 0.05 & 0.05 & 0.05 & 0.05 \\
\end{array}$$
#+end_quote

** Solution

#+begin_src julia :results output
function optimal_bst(p, q, n)
    e = zeros(n + 1, n + 1)                             # e = expected cost matrix
    w = zeros(n + 1, n + 1)                             # w = weight matrix (+1 for dummies)
    root = zeros(Int, n, n)                             # root = optimal root node

    for i in 1:n+1
        e[i, i] = q[i]                                  # base: empty subtree
        w[i, i] = q[i]
    end

    for l in 1:n                                        # l = length of the sequence
        for i in 1:n-l+1                                # i = start of the sequence
            j = i + l - 1                               # j = end of the sequence
            e[i, j+1] = Inf
            w[i, j+1] = w[i, j] + p[j] + q[j+1]         # weight of [i,j]: all keys + dummies
            for r in i:j                                # r = trial root for keys k[i..j]
                t = e[i, r] + e[r+1, j+1] + w[i, j+1]   # t = temporary cost
                if t < e[i, j+1]                        # keep root r if it lowers the cost
                    e[i, j+1] = t
                    root[i, j] = r
                end
            end
        end
    end

    return e, root
end

function construct_optimal_bst(r, n)
    function build(i, j, parent, dir)
        if i > j
            println("d", i - 1, " is the ", dir, " child of k", parent)
            return
        end
        t = r[i, j]
        if parent == 0
            println("k", t, " is the root")
        else
            println("k", t, " is the ", dir, " child of k", parent)
        end
        build(i, t - 1, t, "left")
        build(t + 1, j, t, "right")
    end
    build(1, n, 0, "")
end


p =       [0.04, 0.06, 0.08, 0.02, 0.10, 0.12, 0.14]
q = [0.06, 0.06, 0.06, 0.06, 0.05, 0.05, 0.05, 0.05]
n = 7

e, root = optimal_bst(p, q, n)
println("cost = ", round(e[1, n+1], digits=4))
construct_optimal_bst(root, n)
#+end_src

#+RESULTS:
#+begin_example
cost = 3.12
k5 is the root
k2 is the left child of k5
k1 is the left child of k2
d0 is the left child of k1
d1 is the right child of k1
k3 is the right child of k2
d2 is the left child of k3
k4 is the right child of k3
d3 is the left child of k4
d4 is the right child of k4
k7 is the right child of k5
k6 is the left child of k7
d5 is the left child of k6
d6 is the right child of k6
d7 is the right child of k7
#+end_example

* Problem 92 :dynamic_programming:greedy_algorithm:bitonic_tour:
** Task
#+begin_quote
In the Euclidean traveling-salesperson problem, you are given a set of $n$ points in the plane, and your goal is to find the shortest closed tour that connects all $n$ points. The general problem is NP-hard, as discussed in the lecture course, and its solution is therefore believed to require more than polynomial time. J. L. Bentley has suggested simplifying the problem by considering only bitonic tours, that is, tours that start at the leftmost point, go strictly rightward to the right-most point, and then go strictly leftward back to the starting point, see also Figure 14.11 on page 408. In this case, a polynomial-time algorithm is possible.

[GRAPHIC: Figure 14.11 — illustration of a bitonic tour on a set of points in the plane]

Describe an $O(n^2)$ -time algorithm for determining an optimal bitonic tour. You may assume that no two points have the same $x$ -coordinate and that all operations on real numbers take unit time. (Hint: Scan left to right, maintaining optimal possibilities for the two parts of the tour.)
#+end_quote

** Solution

Sort the points by $x$-coordinate: $p_1, \dots, p_n$. Distinct $x$ makes $p_1$ the unique leftmost and $p_n$ the unique rightmost point. A bitonic tour is exactly a pair of strictly left-to-right paths from $p_1$ to $p_n$ whose union covers every point (the rightward leg and the leftward leg). Minimizing the tour length is minimizing the combined length of this pair of paths.

*Subproblem.* For $1 \leq i \leq j$, let $b[i, j]$ be the minimum combined length of two left-to-right paths that both start at $p_1$, end at $p_i$ and $p_j$ respectively, and together cover exactly $p_1, \dots, p_j$. By symmetry assume $i \leq j$, so $p_j$ is the rightmost covered point and is an endpoint.

*Recurrence.* Write $d(a, b)$ for the Euclidean distance. The two rightmost covered points are $p_{j-1}$ and $p_j$, which drives the case split:

- If $i < j - 1$, then $p_{j-1}$ is not an endpoint, so it lies in the interior of the path ending at $p_j$; being the rightmost interior point, it must be the immediate predecessor of $p_j$. Deleting $p_j$ leaves the configuration $(i, j-1)$:
  $$b[i, j] = b[i, j-1] + d(p_{j-1}, p_j).$$

- If $i = j - 1$, then $p_{j-1}$ and $p_j$ are both endpoints, so the predecessor of $p_j$ may be any $p_k$ with $k < j - 1$; take the best:
  $$b[j-1, j] = \min_{1 \leq k < j-1}\{\, b[k, j-1] + d(p_k, p_j) \,\}.$$

Base cases: $b[1, 1] = 0$ and $b[1, 2] = d(p_1, p_2)$.

The optimal bitonic tour closes both paths at $p_n$, so its length is
$$b[n-1, n] + d(p_{n-1}, p_n).$$

#+begin_example
EUCLIDEAN-BITONIC-TOUR(p, n)              // p[1..n] sorted by x-coordinate
  b[1, 1] = 0
  for j = 2 to n
    for i = 1 to j - 1
      if i < j - 1
        b[i, j] = b[i, j-1] + d(p[j-1], p[j])
        par[i, j] = j - 1
      else                                // i == j - 1
        b[i, j] = ∞
        for k = 1 to j - 2
          q = b[k, j-1] + d(p[k], p[j])
          if q < b[i, j]
            b[i, j] = q
            par[i, j] = k
  return b[n-1, n] + d(p[n-1], p[n])
#+end_example

*Running time.* The pair $(i, j)$ ranges over $\Theta(n^2)$ cells. Every cell with $i < j-1$ costs $O(1)$. The inner minimization, costing $O(j)$, runs only for the single cell $i = j-1$ of each $j$, contributing $\sum_{j} O(j) = O(n^2)$. Total $\Theta(n^2)$.

The actual tour is recovered from $par$: tracing $par[i, j]$ back from the closing pair splits the points into the two paths.

* Problem 93 :dynamic_programming:tree:greedy_algorithm:
** Task
#+begin_quote
Professor Blutarsky is consulting for the president of a corporation that is planning a company party. The company has a hierarchical structure, that is, the supervisor relation forms a tree rooted at the president. The human resources department has ranked each employee with a conviviality rating, which is a real number. In order to make the party fun for all attendees, the president does not want both an employee and his or her immediate supervisor to attend. Professor Blutarsky is given the tree that describes the structure of the corporation, using the left-child, right-sibling representation described in Section 10.3. Each node of the tree holds, in addition to the pointers, the name of an employee and that employee's conviviality ranking. Describe an algorithm to make up a guest list that maximizes the sum of the conviviality ratings of the guests. Analyze the running time of your algorithm.
#+end_quote

** Solution

A valid guest list is an *independent set* in the supervisor tree: no node and its parent are both chosen. The goal is the maximum-weight independent set, with weights the conviviality ratings.

*Subproblem.* For each node $v$, compute two values over the subtree rooted at $v$:

- $A[v]$ — maximum total conviviality when $v$ *attends*.
- $B[v]$ — maximum total conviviality when $v$ *stays home*.

*Recurrence.* Over the children $c$ of $v$:
$$A[v] = v.conviv + \sum_{c} B[c], \qquad B[v] = \sum_{c} \max(A[c], B[c]).$$
If $v$ attends, none of its children may; if $v$ stays home, each child independently takes its better option. A leaf has $A[v] = v.conviv$ and $B[v] = 0$. The answer is $\max(A[T.root], B[T.root])$ — the president has no supervisor, so the root is unconstrained.

In the left-child, right-sibling representation, the children of $v$ are enumerated by following $v.\textit{left-child}$ and then $.\textit{right-sibling}$ pointers.

#+begin_example
PARTY(v)                                  // returns (attend, stay) for subtree at v
  if v == NIL
    return (0, 0)
  attend = v.conviv                       // v attends: every child stays home
  stay = 0                                // v stays: each child takes its best
  c = v.left-child
  while c != NIL
    (a, s) = PARTY(c)
    attend = attend + s
    stay = stay + max(a, s)
    c = c.right-sibling
  return (attend, stay)

MAX-CONVIVIALITY(T)
  (a, s) = PARTY(T.root)
  return max(a, s)
#+end_example

*Running time.* Each node is the argument of exactly one =PARTY= call, doing $O(1)$ work plus one walk over its children via sibling pointers. Each node is walked over as a child exactly once, so the total is $\Theta(n)$.

To produce the list itself, run a top-down pass: invite $v$ iff $A[v] \geq B[v]$ and $v$'s parent was not invited.

* Problem 94 :greedy_algorithm:activity_selection:correctness_proof:
** Task
#+begin_quote
Consider the activity selection (room scheduling) problem presented in the lecture course. Suppose that instead of always selecting the first activity to finish, you instead select the last activity to start that is compatible with all previously selected activities. Describe how this approach is a greedy algorithm, and prove that it yields an optimal solution.
#+end_quote

** Solution

Activities $a_1, \dots, a_n$ have start times $s_i$ and finish times $f_i$; two are *compatible* if their intervals do not overlap. Sort so that $s_1 \geq s_2 \geq \dots \geq s_n$. The rule picks $a_1$, then repeatedly the latest-starting activity compatible with everything already chosen.

*Why it is greedy.* It builds the solution one activity at a time, each step committing to the locally optimal choice — the largest available start time — and never reconsidering. The single greedy criterion is "maximize the start time of the next pick."

*Lemma (greedy-choice property).* Some maximum-size set of mutually compatible activities contains $a_1$, the activity with the latest start time.

*Proof.*

1. Let $O$ be a maximum-size compatible set, and let $a_m \in O$ be the activity in $O$ with the latest start time.

2. If $a_m = a_1$, done. Otherwise $s_1 \geq s_m$, since $a_1$ starts latest of all.

3. Take any other $a_i \in O \setminus \{a_m\}$. By choice of $a_m$, $s_i \leq s_m$, hence $s_i < f_m$, so the option $f_m \leq s_i$ in the compatibility of $a_i$ with $a_m$ is impossible; the other option must hold: $f_i \leq s_m$.

4. Then $f_i \leq s_m \leq s_1$, so $a_1$ is compatible with every $a_i \in O \setminus \{a_m\}$.

5. Thus $O' = (O \setminus \{a_m\}) \cup \{a_1\}$ is compatible and $|O'| = |O|$, so $O'$ is optimal and contains $a_1$. $\qquad\blacksquare$

*Optimal substructure.* After committing to $a_1$, the still-selectable activities are exactly $S' = \{a_i : f_i \leq s_1\}$. An optimal solution containing $a_1$ is $a_1$ together with an optimal solution of the subproblem on $S'$: were the remainder not optimal for $S'$, replacing it with a larger compatible subset of $S'$ would enlarge $O'$, a contradiction.

*Conclusion.* Induct on the number of activities. The greedy first choice $a_1$ lies in some optimum (Lemma), and greedy solves the residual problem $S'$ optimally (induction hypothesis with optimal substructure). Hence the latest-start rule returns a maximum-size compatible set.

Step 3 is the load-bearing one: it converts "$a_m$ is the latest-starting member of $O$" into "$a_1$ clears every member of $O$", which is exactly what licenses the swap.

* Problem 95 :greedy_algorithm:activity_selection:counterexample:
** Task
#+begin_quote
Consider again the activity selection (room scheduling) problem. Not just any greedy approach to the activity-selection problem produces a maximum-size set of mutually compatible activities. Give an example to show that the approach of selecting the activity of least duration from among those that are compatible with previously selected activities does not work. Do the same for the approaches of always selecting the compatible activity that overlaps the fewest other remaining activities and always selecting the compatible remaining activity with the earliest start time.
#+end_quote

** Solution

Activities are intervals $[s, f)$; two are compatible iff $f_i \leq s_j$ or $f_j \leq s_i$.

*** Least duration

Activities $[0, 5)$, $[4, 6)$, $[5, 10)$. The shortest is $[4, 6)$ (length 2), but it overlaps both others, so the rule selects only it — size 1. The optimum $\{[0, 5),\, [5, 10)\}$ has size 2.

*** Fewest overlaps with remaining activities

This rule is more robust than it looks: under the per-step recount no counterexample exists for fewer than about ten activities. The following eleven do break it:
$$[1,4),\ [1,5),\ [2,4),\ [2,6),\quad [4,8),\ [6,9),\ [8,10),\quad [9,13),\ [9,14),\ [10,15),\ [11,14).$$
The bridge $[6, 9)$ overlaps exactly two activities ($[4, 8)$ and $[8, 10)$), while every other activity overlaps at least three. So $[6, 9)$ is the *unique* minimum and the rule must select it first, regardless of tie-breaking. Selecting it discards $[4, 8)$ and $[8, 10)$; afterwards the left block $\{[1,4), [1,5), [2,4), [2,6)\}$ and the right block $\{[9,13), [9,14), [10,15), [11,14)\}$ are each a clique, so at most one survives from each — the rule finishes with size 3. The optimum avoids the bridge entirely:
$$\{[1, 4),\ [4, 8),\ [8, 10),\ [10, 15)\},$$
of size 4. The chain $[4,8)$–$[6,9)$–$[8,10)$ flanked by two cliques is what defeats the rule.

*** Earliest start time

Activities $[0, 10)$, $[1, 2)$, $[3, 4)$. The earliest-starting $[0, 10)$ blocks everything else, so the rule selects only it — size 1. The optimum $\{[1, 2),\, [3, 4)\}$ has size 2.

* Problem 96 :greedy_algorithm:interval_graph_coloring:scheduling:
** Task
#+begin_quote
You are given a set of activities to schedule among a large number of lecture halls, where any activity can take place in any lecture hall. You wish to schedule all the activities using as few lecture halls as possible. Give an efficient greedy algorithm to determine which activity should use which lecture hall. (This problem is also known as the interval-graph coloring problem. It is modeled by an interval graph whose vertices are the given activities and whose edges connect incompatible activities. The smallest number of colors required to color every vertex so that no two adjacent vertices have the same color corresponds to finding the fewest lecture halls needed to schedule all of the given activities.)
#+end_quote

** Solution

*Lower bound.* At any instant, all activities then in progress are pairwise incompatible and need distinct halls. So if $D$ is the maximum number of activities in progress simultaneously (the maximum overlap depth), at least $D$ halls are required.

*Algorithm.* Process activities in order of start time, maintaining a pool of halls each tagged with the finish time of its current occupant. For each activity, if some hall's occupant has already finished by this start time, reuse that hall; otherwise open a new one. A min-heap of halls keyed by occupant finish time gives the earliest-freeing hall in $O(\lg n)$.

#+begin_example
SCHEDULE-HALLS(s, f, n)                    // activities sorted: s[1] ≤ ... ≤ s[n]
  H = empty min-heap keyed by free-time    // free-time = finish of current occupant
  halls = 0
  for i = 1 to n
    if H is not empty and MINIMUM(H).free-time ≤ s[i]
      h = EXTRACT-MIN(H)                    // reuse a freed hall
    else
      halls = halls + 1                     // open a new hall
      h = new hall with id halls
    assign a[i] to hall h
    h.free-time = f[i]
    INSERT(H, h)
  return halls
#+end_example

*Correctness.* A new hall is opened only when every existing hall is still busy at time $s_i$ — i.e. $a_i$ together with one occupant per busy hall forms a set of activities all containing the instant $s_i$, hence mutually overlapping. So whenever the hall count reaches $k$, some $k$ activities overlap at a point, forcing $\geq k$ halls. The algorithm therefore never exceeds the lower bound $D$, and it plainly seats everyone, so it uses exactly $D$ — the optimum.

*Running time.* Sorting by start time is $O(n \lg n)$; each activity performs $O(1)$ heap operations at $O(\lg n)$ each, so $O(n \lg n)$ overall.

* Problem 97 :dynamic_programming:activity_selection:weighted_scheduling:
** Task
#+begin_quote
Consider a modification to the activity-selection problem in which each activity $a_i$ has, in addition to a start and finish time, a value $v_i$. The objective is no longer to maximize the number of activities scheduled, but instead to maximize the total value of the activities scheduled. That is, the goal is to choose a set $A$ of compatible activities such that $\sum_{a_k \in A} v_k$ is maximized. Give a polynomial-time algorithm for this problem.
#+end_quote

** Solution

The greedy rules fail once activities carry values, so this needs dynamic programming. Sort the activities by finish time, $f_1 \leq \dots \leq f_n$. Define $p(i)$ as the largest index $j < i$ with $f_j \leq s_i$ — the last activity finishing no later than $a_i$ starts — or $0$ if none exists.

*Subproblem.* $W[i]$ = maximum total value obtainable using only $a_1, \dots, a_i$. Activity $a_i$ is either excluded (value $W[i-1]$) or included, in which case all activities indexed between $p(i)+1$ and $i-1$ conflict with it and are unavailable, leaving value $v_i + W[p(i)]$:
$$W[i] = \max\big(W[i-1],\ v_i + W[p(i)]\big), \qquad W[0] = 0.$$
The answer is $W[n]$.

#+begin_example
WEIGHTED-ACTIVITY(s, f, v, n)              // sorted by finish: f[1] ≤ ... ≤ f[n]
  compute p[1..n]                          // p[i] = max{ j < i : f[j] ≤ s[i] }, else 0
  W[0] = 0
  for i = 1 to n
    W[i] = max(W[i-1], v[i] + W[p[i]])
  return W[n]
#+end_example

*Running time.* Sorting by finish time is $O(n \lg n)$. Each $p(i)$ comes from a binary search over the sorted finish times, $O(\lg n)$, so $O(n \lg n)$ for all of them; a single linear merge would also give all $p(i)$ in $O(n)$. The DP loop is $O(n)$. Total $O(n \lg n)$ — polynomial. The chosen set is recovered by walking $W$ backward: $a_i$ is in the solution iff $v_i + W[p(i)] > W[i-1]$.

* Problem 98 :greedy_algorithm:knapsack:correctness_proof:
** Task
#+begin_quote
Prove that the fractional knapsack problem has the greedy-choice property. Produce an algorithm that solves the problem in $O(n)$ time.
#+end_quote

** Solution

Items $1, \dots, n$ have value $v_i > 0$ and weight $w_i > 0$; the capacity is $W$. A solution takes $x_i \in [0, w_i]$ of each item with $\sum_i x_i \leq W$, maximizing $\sum_i (v_i / w_i)\, x_i$. The *density* of item $i$ is $v_i / w_i$; let item 1 have maximum density.

*** Greedy-choice property

*Claim.* Some optimal solution takes $x_1 = \min(w_1, W)$ — as much of the densest item as fits.

*Proof* (exchange).

1. Let $x = (x_1, \dots, x_n)$ be optimal, and suppose $x_1 < \min(w_1, W)$ (otherwise done).

2. If $\sum_i x_i < W$ there is spare capacity, and raising $x_1$ strictly increases the objective (density is positive), contradicting optimality. So $\sum_i x_i = W$, and since $x_1 < \min(w_1, W) \leq W$, some other item $j$ has $x_j > 0$.

3. Let $\delta = \min\big(\min(w_1, W) - x_1,\ x_j\big) > 0$. Set $x_1' = x_1 + \delta$, $x_j' = x_j - \delta$, all else unchanged. This is feasible: total weight stays $W$, and $0 \leq x_1' \leq \min(w_1, W)$, $0 \leq x_j'$.

4. The objective changes by $\delta\,(v_1/w_1 - v_j/w_j) \geq 0$, since item 1 has maximum density. So $x'$ is also optimal, with strictly more of item 1.

5. Repeating raises $x_1$ to $\min(w_1, W)$ without ever lowering the objective, giving an optimal solution with $x_1 = \min(w_1, W)$. $\qquad\blacksquare$

*Optimal substructure.* After taking $\min(w_1, W)$ of item 1, what remains is a fractional knapsack on items $2, \dots, n$ with capacity $W - x_1$; an optimal completion appended to the greedy choice is optimal overall.

*** Linear-time algorithm

Sorting by density costs $O(n \lg n)$. Linear time comes from partitioning around the *median* density (found with linear-time selection) instead of sorting.

#+begin_example
FRACTIONAL-KNAPSACK(items, W)
  if items is empty or W ≤ 0
    return 0
  ρ = median of { v_i / w_i : i ∈ items }   // linear-time SELECT
  G = { i ∈ items : v_i/w_i > ρ }           // three-way partition, O(n)
  E = { i ∈ items : v_i/w_i == ρ }
  L = { i ∈ items : v_i/w_i < ρ }
  W_G = total weight of G
  if W_G > W
    return FRACTIONAL-KNAPSACK(G, W)         // optimum lies entirely within G
  value = total value of G                   // take all of G, using W_G
  cap = W - W_G
  fill from E up to cap, adding the value taken and reducing cap
  if cap > 0
    value = value + FRACTIONAL-KNAPSACK(L, cap)
  return value
#+end_example

*Correctness.* Applying the greedy-choice property repeatedly, an optimal solution fills items in nonincreasing density order. Every item of $G$ outranks every item of $E \cup L$, so all of $G$ is taken first. If $W_G > W$ the optimum never reaches $E \cup L$ and the problem reduces to $G$ with capacity $W$. Otherwise all of $G$ is taken; the boundary-density items $E$ are mutually interchangeable and fill the next slice of capacity; any leftover is filled from $L$, a strictly smaller subproblem.

*Running time.* Median selection and the three-way partition are $O(n)$. Because $\rho$ is the median, $|G| \leq \lfloor n/2 \rfloor$ and $|L| \leq \lfloor n/2 \rfloor$, and the single recursive call runs on one of them:
$$T(n) = T(n/2) + O(n) = O(n).$$

* Problem 99 :dynamic_programming:knapsack:
** Task
#+begin_quote
Give a dynamic-programming solution of the $0 - 1$ knapsack problem that runs in $O(nW)$ time, where $n$ is the number of items and $W$ is the maximum weight of items that the thief can put in the knapsack.
#+end_quote

** Solution

Items $1, \dots, n$ have weight $w_i$ and value $v_i$; the capacity is $W$. Each item is taken whole or not at all.

*Subproblem.* $c[i, w]$ = maximum value obtainable from a subset of the first $i$ items whose total weight is at most $w$.

*Recurrence.* Item $i$ is either skipped, or taken when it fits:
$$c[i, w] = \begin{cases}
0 & i = 0 \text{ or } w = 0, \\
c[i-1, w] & w_i > w, \\
\max\big(c[i-1, w],\ v_i + c[i-1, w - w_i]\big) & w_i \leq w.
\end{cases}$$
The answer is $c[n, W]$.

#+begin_example
KNAPSACK-01(w, v, n, W)
  for j = 0 to W
    c[0, j] = 0
  for i = 1 to n
    for j = 0 to W
      if w[i] > j
        c[i, j] = c[i-1, j]
      else
        c[i, j] = max(c[i-1, j], v[i] + c[i-1, j - w[i]])
  return c[n, W]
#+end_example

*Running time.* The table has $(n+1)(W+1)$ entries, each filled in $O(1)$, so $\Theta(nW)$. This is pseudo-polynomial: linear in the value of $W$ but exponential in its bit-length. The chosen items are recovered from $c$: item $i$ was taken iff $c[i, w] \neq c[i-1, w]$, after which $w_i$ is subtracted and the walk continues from $c[i-1, w - w_i]$.

* Time Estimates
- Problem 90: ~18 min~
- Problem 91: ~50 min~
- Problem 92: ~45 min~
- Problem 93: ~30 min~
- Problem 94: ~30 min~
- Problem 95: ~28 min~
- Problem 96: ~28 min~
- Problem 97: ~28 min~
- Problem 98: ~40 min~
- Problem 99: ~17 min~
