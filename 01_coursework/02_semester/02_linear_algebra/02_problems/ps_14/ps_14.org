#+title: Ps 14
#+author: Erik An
#+email: obluda2173@gmail.com
#+date: <2026-06-20>
#+lastmod: <2026-06-20 11:55>
#+latex: \newpage
#+latex_header: \setlength{\parindent}{0pt}
#+latex_header: \setlength{\parskip}{1em}
#+latex_header: \usepackage{amsmath}
#+latex_header: \usepackage{amssymb}
#+latex_header: \usepackage{mathtools}
#+latex_header: \usepackage{amsthm}
#+latex_header: \usepackage[margin=1in]{geometry}
#+options: num:t tags:nil
#+property: header-args :eval never-export
#+startup: overview latexpreview inlineimages
#+columns: %50ITEM(Item) %8LECTURE_REF(Lecture) %34CUSTOM_ID(ID)

* Problem 5.22 :diagonalization:linear_map:polynomial:
** Task
#+begin_quote
Let $\mathbb{E} = \mathbb{R}_4[X]$, and let $L : \mathbb{E} \to \mathbb{E}$ be defined as $L(P) := P' + P''$. Determine whether $L$ is diagonalizable.
#+end_quote

** Solution

* Problem 5.25 :matrix_exponential:commutativity:binomial_theorem:
** Task
#+begin_quote
Let $A, B \in \mathbb{C}^{n\times n}$.

(i) Prove that if $AB = BA$ then the /Binomial theorem/ holds:

$$(A + B)^n = \sum_{k=0}^{n} A^i B^{n-i}$$.

(ii) Prove that if $AB = BA$ then $\exp(A + B) = \exp(A) \exp(B)$ (*Indication:* /Cauchy Product/: $\left(\sum_{i=0}^{\infty} a_i\right)\left(\sum_{j=0}^{\infty} b_j\right) = \sum_{k=0}^{\infty} c_k$ with $c_k = \sum_{h=0}^{k} a_{k-h} b_h$).

(iii) Is the reverse implication true (i.e. for matrices that satisfy $\exp(A) \exp(B) = \exp(A + B)$ we always have $AB = BA$)?
#+end_quote

** Solution
*** (i)

*** (ii)

*** (iii)

* Problem 5.29 :nilpotent:characteristic_polynomial:diagonalization:
** Task
#+begin_quote
Let $P(X)$ be a polynomial and $A \in \mathbb{K}^{n\times n}$, we define $B := P(A) \in \mathbb{K}^{n\times n}$. Prove for eigenvector $v$ of $A$ with eigenvalues $\lambda$, then $v$ is also an eigenvector of $B$ with eigenvalue $P(\lambda)$. Apply this result to prove:

(i) If $A \neq 0$ is nilpotent, then $A$ is not diagonalizable.

(ii) If $A$ is diagonalizable, then $p_A(A) = 0^{n\times n}$, i.e. substituting the matrix in its characteristic polynomial, the resulting matrix is the zero matrix. (*Note:* Here we have assumed $A$ to be diagonalizable, but the result can be generalized to all square matrices, which is the Cayley-Hamilton Theorem.)
#+end_quote

** Solution
*** (i)

*** (ii)

* Problem 5.31 :diagonalization:eigenvalue:recurrence_relation:
** Task
#+begin_quote
Let $x \in \mathbb{R}$ be a fixed parameter. Consider the matrix

$$A(x) = \begin{pmatrix} 2x & -1 \\ 1 & 0 \end{pmatrix}$$.

Some well known families of polynomials can be generated by recurrence relations that share this underlying matrix structure. In this exercise, we will explore two such families.

(i) Obtain an eigenbasis for $A(x)$ and therefore diagonalize $A(x)$ in terms of its eigenvalues $\lambda_-(x)$ and $\lambda_+(x)$.

(ii) Consider a general sequence of functions $P_n(x)$ satisfying the recurrence relation $P_{n+1}(x) = 2x P_n(x) - P_{n-1}(x)$ for $n \geq 1$. Express the vector $(P_n(x), P_{n-1}(x))^T$ in terms of $A(x)$, $n$, and the initial conditions $P_1(x)$ and $P_0(x)$.

(iii) Let $T_n(x)$ denote the Chebyshev polynomials of the /first kind/, defined by the initial conditions $T_0(x) = 1$ and $T_1(x) = x$. Using the diagonalization from (i), obtain the closed algebraic form of $T_n(x)$.

(iv) By restricting $x \in [-1, 1]$ and making the substitution $x = \cos\theta$, show that $T_n(\cos\theta) = \cos(n\theta)$ (*Indication:* de Moivre's formula.)

(v) Let $U_n(x)$ denote the Chebyshev polynomials of the /second kind/, defined by the initial conditions $U_0(x) = 1$ and $U_1(x) = 2x$. Using the diagonalization from (i), obtain the closed algebraic form of $U_n(x)$.

(vi) Using the same substitution $x = \cos\theta$, show that $U_n(\cos\theta) = \frac{\sin((n+1)\theta)}{\sin\theta}$.
#+end_quote

** Solution
*** (i)

*** (ii)

*** (iii)

*** (iv)

*** (v)

*** (vi)

* Errors/Typos
- Problem 5.25: summation index "$k$" → "$i$" (the index of summation does not match the summand $A^i B^{n-i}$)
- Problem 5.25: "$\sum_{i=0}^{n} A^i B^{n-i}$" → "$\sum_{i=0}^{n} \binom{n}{i} A^i B^{n-i}$" (binomial coefficient missing from the stated theorem)
- Problem 5.29: "eigenvalues $\lambda$" → "eigenvalue $\lambda$"

* Time Estimates
- Problem 5.22: ~20 min~
- Problem 5.25: ~45 min~
- Problem 5.29: ~35 min~
- Problem 5.31: ~60 min~
